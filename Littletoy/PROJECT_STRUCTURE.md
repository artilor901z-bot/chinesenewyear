# 项目组成部分总结 - 福字交互体验

## 项目概览

**项目名称**: 福 (Fu) - Chinese Fortune Character Interactive Experience
**原始网站**: https://underwater008.github.io/fu/
**本地开发**: http://localhost:5173/fu/
**项目类型**: 3D 交互式 Web 应用
**技术栈**: Three.js + WebGL + Canvas 2D + Vite

---

## 📁 文件结构

```
d:\Littletoy/
│
├── fu/                                    # 核心应用目录
│   ├── index.html                        # 主 HTML 入口
│   ├── fu.js                             # 核心逻辑 (75KB, 约 2300 行)
│   ├── particleVertex.glsl               # 顶点着色器 (1KB)
│   └── particleFragment.glsl             # 片段着色器 (0.6KB)
│
├── node_modules/                          # NPM 依赖包
│
├── index.html                            # 项目主页
├── package.json                          # 项目配置和依赖
├── package-lock.json                     # 依赖锁定文件
├── vite.config.js                        # Vite 构建配置
├── .gitignore                            # Git 忽略规则
├── README.md                             # 项目说明文档
├── USAGE.md                              # 使用指南
└── PROJECT_STRUCTURE.md                  # 本文件
```

---

## 🧩 核心组成部分

### 1️⃣ HTML 层 (fu/index.html)

**功能**: 页面结构和资源加载

**关键元素**:
- `<canvas id="c">` - 主渲染画布
- Google Fonts 引用 (3 种书法字体)
- Chinese Fonts CDN (4 种额外字体)
- 全屏红色背景 (#CC0000)
- 响应式 viewport 配置

**字体资源**:
```
Google Fonts:
  - Liu Jian Mao Cao (柳建毛草)
  - Ma Shan Zheng (马善政)
  - Zhi Mang Xing (志芒星)

Chinese Fonts CDN:
  - LongCang (龙藏)
  - ZCOOL XiaoWei (站酷小薇)
  - ZCOOL QingKeHuangYou (站酷庆科黄油)
  - ZCOOL KuaiLe (站酷快乐)
```

---

### 2️⃣ JavaScript 核心逻辑 (fu/fu.js)

**文件大小**: 75,153 字节
**代码行数**: ~2,300 行
**主要功能**: 状态机、粒子系统、动画、交互

#### 🎯 核心架构

##### A. 状态机 (State Machine)
```javascript
四个主要状态:
├── ARRIVAL   - 到达/欢迎阶段
├── DRAW      - 抽签/爆炸动画
├── FORTUNE   - 显示运势
└── FIREWORKS - 烟花庆祝
```

##### B. 配置对象 (CONFIG)
```javascript
{
  bg: '#990000',              // 背景色
  glowRed: '#FF2D2D',        // 红色发光
  glowGold: '#FFD700',       // 金色发光
  glowGreen: '#00FF9F',      // 绿色发光
  scatterDur: 450,           // 散开时长 (ms)
  scrambleDur: 900,          // 混乱时长 (ms)
  convergeDur: 450,          // 聚合时长 (ms)
  settleDur: 250,            // 稳定时长 (ms)
  fuExplodeDelay: 2.0,       // 爆炸延迟 (s)
  shellRiseDuration: 2.5     // 烟花上升时间 (s)
}
```

##### C. 数据结构

**字符库**:
```javascript
LUCKY_CHARS_BY_DENSITY (25 字符)
├── ' ', '·', '一', '人', '十', '大', '吉', '平', '安', '和'
├── '春', '利', '兴', '旺', '发', '金', '贵', '富', '财', '寿'
└── '禄', '喜', '龙', '凤', '福'

ALL_LUCKY (100+ 字符)
└── '福禄寿喜财富贵发金玉宝余丰盛利旺隆昌兴进安康宁泰和...'
```

**祝福映射**:
```javascript
CHAR_BLESSINGS = {
  '福': { phrase: '福如东海', english: 'Fortune like the Eastern Sea' },
  '寿': { phrase: '寿比南山', english: 'Longevity like the Southern Mountains' },
  '大': { phrase: '大吉大利', english: 'Great luck and great profit' },
  // ... 100+ 条祝福语
}
```

**颜色分类**:
```javascript
BLESSING_CATEGORIES (9 类):
├── 五福 (红色)    - 福禄寿喜财
├── 财富 (金色)    - 金玉宝富贵
├── 平安 (玉色)    - 安康宁泰和
├── 喜庆 (暖色)    - 欢乐庆禧祺
├── 德行 (琥珀)    - 德善仁义忠
├── 爱情 (粉色)    - 恩爱合圆满
├── 吉祥 (柠檬)    - 吉祥瑞如意
├── 神话 (橙色)    - 龙凤麟鹤
└── 成就 (青色)    - 成升登高
```

##### D. 粒子系统

**规格**:
- 最大粒子数: 4,000
- 纹理图集: 20×20 网格 (400 字符容量)
- 字符分辨率: 64×64 像素
- 渲染方式: GPU 实例化

**粒子属性**:
```javascript
每个粒子包含:
├── instanceMatrix     - 位置/旋转矩阵
├── instanceColor      - RGB 颜色 (vec3)
├── instanceAlpha      - 透明度 (float)
├── instanceUV         - 纹理坐标 (vec2)
└── instanceScale      - 缩放系数 (float)
```

##### E. 动画时间线

**DRAW 阶段详细流程**:
```
0.0s  ━━━ 福字开始上升并缩小
0.45s ━━━ 相机快速后退
1.15s ━━━ 相机回归原位
2.0s  ━━━ 💥 爆炸闪光！粒子激活
      ┃
      ┣━━ 粒子向外散射 (随机角度)
      ┣━━ 应用重力和摇摆效果
      ┃
2.45s ━━━ 散射阶段结束
      ┃
      ┣━━ 混乱字符闪烁
      ┣━━ 快速尺寸变化
      ┃
3.2s  ━━━ 开始重组为"大吉"
      ┃
      ┣━━ 谐波脉冲
      ┣━━ 粒子集结
      ┃
3.65s ━━━ 稳定阶段
      ┃
      ┣━━ 颜色混合
      ┣━━ 最终定位
      ┃
4.0s  ━━━ 转换到 FORTUNE 状态
```

##### F. 物理引擎

**烟花粒子物理**:
```javascript
每帧更新:
├── 速度 += 重力加速度 (gravity)
├── 速度 *= 阻力系数 (0.985)
├── 位置 += 速度
├── 透明度 -= 衰减率 (0.008~0.016)
└── 拖尾生成 (每 0.06s 一段, 最多 14 段)
```

##### G. 交互处理

**输入映射**:
```
桌面端:
├── 垂直鼠标拖拽   → 状态切换
├── 鼠标滚轮向上   → 触发下一状态
├── 空格键/↑      → 向上滑动
├── ←/→ 箭头键    → 切换字体
└── 鼠标悬停       → 显示提示

移动端:
├── 垂直滑动 >50px → 状态切换
├── 水平滑动 >50px → 字体切换
└── 触摸悬停       → 提示工具
```

---

### 3️⃣ WebGL Shaders

#### particleVertex.glsl (顶点着色器)

**功能**: Billboard 广告牌粒子渲染

**输入**:
- `attribute vec3 instanceColor` - 粒子颜色
- `attribute float instanceAlpha` - 粒子透明度
- `attribute vec2 instanceUV` - 纹理图集坐标
- `attribute float instanceScale` - 粒子缩放
- `uniform vec2 cellSize` - 图集单元格大小

**输出**:
- `varying vec2 vUv` - 插值后的 UV 坐标
- `varying vec3 vColor` - 插值后的颜色
- `varying float vAlpha` - 插值后的透明度

**核心逻辑**:
```glsl
1. 从 instanceMatrix 提取世界位置
2. 转换到视图空间 (相机空间)
3. Billboard 展开 (始终面向相机)
4. 投影到屏幕空间
5. 计算纹理坐标偏移
```

#### particleFragment.glsl (片段着色器)

**功能**: 采样字符纹理并应用颜色

**输入**:
- `uniform sampler2D atlas` - 字符纹理图集
- `varying vec2 vUv` - UV 坐标
- `varying vec3 vColor` - 颜色
- `varying float vAlpha` - 透明度

**核心逻辑**:
```glsl
1. 采样纹理的 R 通道 (强度)
2. Gamma 校正 (pow 0.75) 提升光晕
3. 计算最终透明度
4. 提前丢弃完全透明像素 (优化)
5. 输出预乘 Alpha 用于加法混合
```

---

### 4️⃣ 依赖库 (package.json)

#### 生产依赖 (dependencies)
```json
{
  "three": "^0.164.1",       // 3D 渲染引擎 (核心)
  "gsap": "^3.12.5",         // 动画库 (高性能)
  "lil-gui": "^0.19.2",      // 调试控制面板
  "lottie-web": "^5.12.2",   // 矢量动画
  "stats-js": "^1.0.1",      // 性能监控 (FPS)
  "gasp": "^0.0.2"           // 额外动画工具
}
```

#### 开发依赖 (devDependencies)
```json
{
  "vite": "^5.4.11",         // 构建工具 (核心)
  "sass": "^1.77.2",         // CSS 预处理器
  "vite-plugin-string": "^1.2.3"  // 字符串导入插件
}
```

---

### 5️⃣ 构建配置 (vite.config.js)

```javascript
export default defineConfig({
  plugins: [],
  build: {
    rollupOptions: {
      input: {
        main: 'index.html',      // 主页入口
        fu: 'fu/index.html'      // 福字页面入口
      }
    }
  }
});
```

**功能**:
- 多页面应用配置
- ES 模块打包
- 开发服务器热更新
- 生产环境优化

---

## 🎨 渲染管线

```
┌─────────────────────────────────────────────────┐
│          混合渲染架构                              │
├─────────────────────────────────────────────────┤
│                                                 │
│  Canvas 2D (背景层)                              │
│  ├── ASCII 网格背景                              │
│  ├── 扫描线效果                                  │
│  └── 文本叠加层                                  │
│                                                 │
│  ▼ globalCompositeOperation: 'lighter'          │
│                                                 │
│  Three.js WebGL (粒子层)                         │
│  ├── GPU 实例化网格                              │
│  ├── 自定义 Shader 材质                          │
│  ├── 纹理图集采样                                │
│  └── 加法混合                                    │
│                                                 │
│  ▼ 合成到 Canvas                                │
│                                                 │
│  最终输出                                        │
│  └── 发光粒子 + ASCII 背景                       │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🔧 核心算法

### 1. 3D 投影算法
```javascript
function project3D(x, y, z, fov) {
  const scale = fov / (fov + z);
  return {
    screenX: x * scale + window.innerWidth / 2,
    screenY: y * scale + window.innerHeight / 2,
    scale: scale
  };
}
```

### 2. 缓动函数
```javascript
function easeInOut(t) {
  return t < 0.5
    ? 2 * t * t
    : -1 + (4 - 2 * t) * t;
}
```

### 3. 颜色插值
```javascript
function lerpColor(luminance) {
  return {
    r: 255,
    g: Math.floor(45 + luminance * 170),
    b: Math.floor(45 - luminance * 45)
  };
}
```

### 4. 字符形状采样
```
1. 在离屏 Canvas 绘制文字
2. 读取像素数据
3. 根据亮度阈值采样点
4. 归一化坐标 (-1 ~ 1)
5. 返回点云数据
```

---

## 🎯 性能优化策略

| 优化项 | 实现方式 | 效果 |
|--------|----------|------|
| **GPU 实例化** | THREE.InstancedMesh | 减少 Draw Call 到 1 次 |
| **紧凑数组剔除** | In-place 数组操作 | 避免 GC 压力 |
| **DPI 限制** | 最大 2x devicePixelRatio | 降低像素填充率 |
| **提前丢弃** | Shader 中 `discard` | 减少片段处理 |
| **加法混合** | `gl.blendFunc(ONE, ONE)` | 减少过度绘制 |
| **禁用视锥剔除** | `frustumCulled = false` | 粒子体积小,剔除开销大 |
| **纹理图集** | 400 字符单张纹理 | 减少纹理切换 |
| **预计算 UV** | 启动时计算映射 | 运行时零计算 |

---

## 📊 技术指标

```
代码统计:
├── JavaScript:  ~2,300 行
├── GLSL:        ~50 行
├── HTML/CSS:    ~45 行
└── 总计:        ~2,395 行

资源占用:
├── fu.js:       75 KB
├── Three.js:    ~580 KB (压缩)
├── GSAP:        ~50 KB (压缩)
└── 字体:        动态加载 (CDN)

运行时性能:
├── 粒子数:      0 ~ 4,000
├── FPS 目标:    60 FPS
├── 内存占用:    ~50 MB
└── GPU 占用:    中等
```

---

## 🌐 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|--------|----------|------|
| Chrome | 90+ | ✅ 完全支持 |
| Firefox | 88+ | ✅ 完全支持 |
| Safari | 14+ | ✅ 完全支持 |
| Edge | 90+ | ✅ 完全支持 |

**必需功能**:
- WebGL 2.0
- ES6 模块
- Canvas 2D
- 触摸事件 (移动端)

---

## 🚀 启动流程

```
1. npm install        ← 安装依赖
2. npm run dev        ← 启动开发服务器
3. 访问 localhost:5173/fu/
4.
   初始化流程:
   ├── 加载字体资源
   ├── 创建 Three.js 场景
   ├── 生成字符纹理图集
   ├── 初始化粒子系统
   ├── 设置事件监听
   └── 启动动画循环
```

---

## 📦 构建产物

```bash
npm run build
```

**输出结构**:
```
dist/
├── index.html
├── fu/
│   └── index.html
├── assets/
│   ├── fu-[hash].js
│   ├── index-[hash].js
│   └── [vendor]-[hash].js
└── [其他静态资源]
```

---

## 🎓 学习价值

这个项目展示了:
✅ WebGL 粒子系统实现
✅ GPU 实例化渲染
✅ 自定义 Shader 编写
✅ 状态机设计模式
✅ Canvas 2D + WebGL 混合渲染
✅ 物理引擎基础
✅ 响应式交互设计
✅ 性能优化最佳实践

---

## 📝 总结

**项目核心**:
一个基于 Three.js 和 WebGL 的交互式 3D 粒子系统，通过状态机驱动四个动画阶段，使用 GPU 实例化渲染最多 4000 个中文字符粒子，结合物理引擎实现爆炸、烟花等特效，配合 7 种书法字体和 100+ 吉祥字符库，创造独特的中国福字抽签体验。

**技术亮点**:
混合渲染、GPU 加速、自定义 Shader、纹理图集、状态机、物理模拟

**适用场景**:
节日祝福、文化展示、交互艺术、WebGL 学习

---

🎊 **祝你好运！**
